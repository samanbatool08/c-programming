#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void) 
{
    // get a string
    string o = get_string("o: ")

    // copy string's address
    string v = o

    // capitalize first letter in string
    if (strlen(t) > 0)
    {
        t[0] = toupper(t[0]);
    }

    // print string twice (orig & new)
    printf("o: %s\n", o)
    printf("v: %s\n", v)
}